const assert = require("assert");
const fs = require("fs");
const path = require("path");

const root = path.join(__dirname, "..");
const baseUrl = process.env.PAIPACHI_BASE_URL || "http://localhost:8788";

function readLocal(file) {
  return fs.readFileSync(path.join(root, file), "utf8");
}

function assertIncludes(source, needle, label) {
  assert.ok(source.includes(needle), `${label} missing: ${needle}`);
}

function assertExcludes(source, needle, label) {
  assert.ok(!source.includes(needle), `${label} should not contain: ${needle}`);
}

function countMatches(source, pattern) {
  return (source.match(pattern) || []).length;
}

async function fetchText(url) {
  const response = await fetch(url, { cache: "no-store" });
  const text = await response.text();
  return { response, text };
}

function checkHtml(html, label) {
  assertIncludes(html, 'const APP_VERSION = "paipachi-app-v214"', `${label} app version`);
  assertIncludes(html, '"001", "002", "003", "004", "005", "006", "007", "008", "009", "010"', `${label} numbered tester accounts`);
  assertIncludes(html, '皜祈岫?∪董??(001-010 / Lynn / Leia / Andrew / Sally)', `${label} numbered tester login copy`);
  assertIncludes(html, 'id="dailyCoreCard"', `${label} daily core card`);
  assertIncludes(html, 'id="dailyCoreScore"', `${label} daily core score`);
  assertIncludes(html, 'id="dailyCoreProgressFill"', `${label} daily core progress`);
  assertIncludes(html, 'id="dailyCoreStatus"', `${label} daily core status`);
  assertIncludes(html, 'id="dailyCoreMission"', `${label} daily core mission`);
  assertIncludes(html, 'id="dailyCoreActionHub"', `${label} daily core action hub`);
  assertIncludes(html, 'id="dailyThreeMealRhythmCard"', `${label} three-meal rhythm card`);
  assertIncludes(html, 'renderDailyThreeMealRhythmCard', `${label} three-meal rhythm renderer`);
  assertIncludes(html, 'getDailyThreeMealRhythmState', `${label} three-meal rhythm state`);
  assertIncludes(html, '銝??蝧', `${label} three-meal photo habit copy`);
  assertIncludes(html, '"?拚?", "??", "??"', `${label} breakfast lunch dinner rhythm slots`);
  assertIncludes(html, "selectOnboardTone(this, 'maintain')", `${label} maintain body goal option`);
  assertIncludes(html, "selectOnboardTone(this, 'healthy')", `${label} healthy eating goal option`);
  assertIncludes(html, '蝬剜?擃?', `${label} maintain goal copy`);
  assertIncludes(html, '?亙熒憌脤?', `${label} healthy eating goal copy`);
  assertIncludes(html, '蝬剜?擃?撱箄降', `${label} maintain nutrition coach branch`);
  assertIncludes(html, '?亙熒憌脤?撱箄降', `${label} healthy nutrition coach branch`);
  assertIncludes(html, 'id="dailyCoreRoutine"', `${label} daily core routine strip`);
  assertIncludes(html, 'renderDailyCoreActionHub', `${label} daily core action hub renderer`);
  assertIncludes(html, 'renderDailyCoreRoutine', `${label} daily core routine renderer`);
  assertIncludes(html, 'getDailyCoreRoutineItems', `${label} daily core routine items`);
  assertIncludes(html, 'getDailyCoreActionHubItems', `${label} daily core action hub items`);
  assertIncludes(html, 'id="dailyTableCard"', `${label} focused daily table card`);
  assertIncludes(html, 'renderDailyTableCard', `${label} focused daily table renderer`);
  assertIncludes(html, 'id="courseModeCard"', `${label} course mode card`);
  assertIncludes(html, 'renderCourseModeCard', `${label} course mode renderer`);
  assertIncludes(html, 'startCourseMode', `${label} course mode starter`);
  assertIncludes(html, 'prepareCourseNextPhoto', `${label} course next photo action`);
  assertIncludes(html, 'registerCourseMeal', `${label} course meal persistence`);
  assertIncludes(html, '?????擗?閬′蝞?銝撘?, `${label} course mode product copy`);
  assertIncludes(html, '隞擗?', `${label} daily table product copy`);
  assertIncludes(html, '憌臬??抒?????銝擗?, `${label} daily table after-photo same meal copy`);
  assertIncludes(html, 'getPendingAfterPhotoMeals', `${label} pending after-photo meal helper`);
  assertIncludes(html, '隞予??${pendingAfterMeals.length} 擗鋆ㄞ敺', `${label} daily table pending after-photo count`);
  assertIncludes(html, '鋆???擗?銝??啣?鈭?????, `${label} same-meal after-photo assurance copy`);
  assertIncludes(html, '隞?ㄞ銝餅?蝔?, `${label} daily table flow accessibility label`);
  assertIncludes(html, '?ㄞ?', `${label} daily action eat photo`);
  assertIncludes(html, '憌臬?鋆?', `${label} daily action after photo`);
  assertIncludes(html, '隞擃?', `${label} daily action weight`);
  assertIncludes(html, '???噶', `${label} daily action life`);
  assertIncludes(html, '??隞暻?, `${label} daily action ask food`);
  assertIncludes(html, '??憭?, `${label} daily action today memory`);
  assertIncludes(html, '銝?亥店??銝甇?, `${label} simplified home main mission copy`);
  assertIncludes(html, '銝?剖?雿停?臭?憭拍?銝?乩蝙?典??, `${label} six daily action entry copy`);
  assertIncludes(html, 'label: "??"', `${label} daily ask-food check-in state`);
  assertIncludes(html, 'dailyKey(\'lastCoachQuestion\')', `${label} daily ask-food state persistence key`);
  assertIncludes(html, 'focusTodayWeightQuick', `${label} first-layer weight focus shortcut`);
  assertIncludes(html, 'id="dailyCoreNext"', `${label} daily core next step`);
  assertIncludes(html, 'id="mealPlanResumeCard"', `${label} meal plan resume card`);
  assertIncludes(html, 'renderMealPlanResumeCard', `${label} meal plan resume renderer`);
  assertIncludes(html, 'resumeCurrentMealPlan', `${label} meal plan resume action`);
  assertIncludes(html, 'hasActiveMealPlan', `${label} active meal plan helper`);
  assertIncludes(html, 'id="mealFlowCard"', `${label} current meal flow card`);
  assertIncludes(html, 'renderMealFlowCard', `${label} current meal flow renderer`);
  assertIncludes(html, 'getMealFlowState', `${label} current meal flow state`);
  assertIncludes(html, 'id="mealSpeedPromiseCard"', `${label} 10-second meal speed card`);
  assertIncludes(html, 'renderMealSpeedPromiseCard', `${label} 10-second meal speed renderer`);
  assertIncludes(html, 'getMealSpeedPromiseState', `${label} 10-second meal speed state`);
  assertIncludes(html, '10 蝘摯蝞脣漲', `${label} 10-second meal speed copy`);
  assertIncludes(html, 'AI/?祆?', `${label} local fallback speed step`);
  assertIncludes(html, 'id="foodRecognitionSummaryCard"', `${label} food recognition summary card`);
  assertIncludes(html, 'renderFoodRecognitionSummaryCard', `${label} food recognition summary renderer`);
  assertIncludes(html, 'getFoodRecognitionSummary', `${label} food recognition summary helper`);
  assertIncludes(html, 'AI 憌颲刻???', `${label} food recognition summary copy`);
  assertIncludes(html, '銝餉?憌?', `${label} recognized ingredients copy`);
  assertIncludes(html, 'autoSavedMealDraft', `${label} auto-saved meal draft state`);
  assertIncludes(html, 'autoSaveMealDraft', `${label} auto-saved meal draft writer`);
  assertIncludes(html, 'mealSnapshot', `${label} complete auto-saved meal snapshot`);
  assertIncludes(html, 'id="estimateSaveStatusCard"', `${label} estimate save status card`);
  assertIncludes(html, 'getEstimateSaveStatusState', `${label} estimate save status state`);
  assertIncludes(html, 'renderEstimateSaveStatusCard', `${label} estimate save status renderer`);
  assertIncludes(html, 'estimate-save-status-card', `${label} estimate save status style`);
  assertIncludes(html, '?阮摰', `${label} estimate draft safe copy`);
  assertIncludes(html, '?芣迤撘撣?, `${label} estimate not formally saved copy`);
  assertIncludes(html, '???孵???, `${label} estimate save completion step copy`);
  assertIncludes(html, '撌脰??摮???, `${label} auto-saved analysis copy`);
  assertIncludes(html, '甇??摮脖?憭拙???, `${label} formal meal record copy`);
  assertIncludes(html, '??鈭?鋆ㄞ敺', `${label} after-photo primary flow CTA`);
  assertIncludes(html, '憌臬??扳?鋆???擗?, `${label} after-photo same meal copy`);
  assertIncludes(html, 'id="bottomPhotoAction"', `${label} contextual bottom photo action`);
  assertIncludes(html, 'grid-template-columns: 1fr 1fr 1.25fr 1fr 1fr', `${label} bottom nav has five visible slots`);
  assertIncludes(html, 'handleBottomPhotoAction', `${label} contextual bottom photo handler`);
  assertIncludes(html, 'getBottomPhotoActionState', `${label} contextual bottom photo state`);
  assertIncludes(html, '憌臬?鋆?', `${label} contextual after-photo bottom label`);
  assertIncludes(html, '?脣?擗?', `${label} contextual save-before bottom label`);
  assertIncludes(html, '蝥銝?擗?, `${label} meal plan resume copy`);
  assertIncludes(html, '蝜潛???', `${label} meal plan resume CTA`);
  assertIncludes(html, 'id="todayMemoryPreviewCard"', `${label} today memory preview card`);
  assertIncludes(html, 'renderTodayMemoryPreviewCard', `${label} today memory preview renderer`);
  assertIncludes(html, 'openTodayMemory', `${label} today memory preview action`);
  assertIncludes(html, 'TODAY MEMORY', `${label} today memory preview kicker`);
  assertIncludes(html, '??憭拇?蝝?, `${label} today detail CTA`);
  assertIncludes(html, 'id="tataHomeSnapshotCard"', `${label} first-layer TATA snapshot card`);
  assertIncludes(html, 'renderTataHomeSnapshot', `${label} first-layer TATA snapshot renderer`);
  assertIncludes(html, 'getTataAppearanceForecast', `${label} first-layer TATA appearance forecast helper`);
  assertIncludes(html, 'renderTataAppearanceForecast', `${label} first-layer TATA appearance forecast renderer`);
  assertIncludes(html, '憛?憭??', `${label} TATA appearance forecast aria copy`);
  assertIncludes(html, '??瞍漁', `${label} TATA rescue-to-beauty copy`);
  assertIncludes(html, 'TATA TODAY', `${label} first-layer TATA snapshot kicker`);
  assertIncludes(html, '憛??曉嚗?, `${label} first-layer TATA snapshot title`);
  assertIncludes(html, 'tata-home-snapshot-card', `${label} first-layer TATA snapshot style`);
  assertIncludes(html, 'id="tataCarePulseCard"', `${label} TATA care pulse card`);
  assertIncludes(html, 'getTataCarePulse', `${label} TATA care pulse helper`);
  assertIncludes(html, 'renderTataCarePulseCard', `${label} TATA care pulse renderer`);
  assertIncludes(html, 'TATA CARE', `${label} TATA care pulse kicker`);
  assertIncludes(html, '憛?隞予?喳???', `${label} TATA care next task copy`);
  assertIncludes(html, 'tata-care-pulse-card', `${label} TATA care pulse style`);
  assertIncludes(html, 'id="tataProactiveCoachCard"', `${label} proactive coach card`);
  assertIncludes(html, 'getTataProactiveCoachAlerts', `${label} proactive coach alert helper`);
  assertIncludes(html, 'renderTataProactiveCoachCard', `${label} proactive coach renderer`);
  assertIncludes(html, 'memoryProfile = getPersonalDietMemoryProfile(21)', `${label} proactive coach uses personal memory`);
  assertIncludes(html, '?餈??塚?', `${label} proactive coach recent memory alert`);
  assertIncludes(html, '撣詨?璅∪?嚗?, `${label} proactive coach frequent food alert`);
  assertIncludes(html, '?餈?憌??嗅?憭?, `${label} proactive coach place memory alert`);
  assertIncludes(html, '憛??毀', `${label} proactive coach kicker`);
  assertIncludes(html, '銝餃??毀??', `${label} proactive coach copy`);
  assertIncludes(html, '???憭?閬???憟?, `${label} proactive outside meal alert`);
  assertIncludes(html, 'tata-proactive-coach-card', `${label} proactive coach style`);
  assertIncludes(html, 'id="todayWaterQuickCard"', `${label} first-layer water quick card`);
  assertIncludes(html, 'renderTodayWaterQuickCard', `${label} first-layer water renderer`);
  assertIncludes(html, 'focusTodayWaterQuick', `${label} first-layer water focus shortcut`);
  assertIncludes(html, '?偌瑼Ｘ', `${label} first-layer water card kicker`);
  assertIncludes(html, 'today-water-quick-fill', `${label} first-layer water progress style`);
  assertIncludes(html, 'id="todayWeightQuickCard"', `${label} first-layer weight quick card`);
  assertIncludes(html, 'id="todayWeightQuickInput"', `${label} first-layer weight quick input`);
  assertIncludes(html, 'renderTodayWeightQuickCard', `${label} first-layer weight renderer`);
  assertIncludes(html, 'recordTodayWeight', `${label} shared daily weight recorder`);
  assertIncludes(html, 'saveTodayWeightQuick', `${label} first-layer weight save action`);
  assertIncludes(html, '隞擃??舀????, `${label} first-layer daily weight date-bound copy`);
  assertIncludes(html, 'id="todayLifeQuickCard"', `${label} first-layer life quick card`);
  assertIncludes(html, 'id="todaySleepQuickInput"', `${label} first-layer sleep quick input`);
  assertIncludes(html, 'id="todayBowelQuickInput"', `${label} first-layer bowel quick input`);
  assertIncludes(html, 'id="todayEnergyQuickInput"', `${label} first-layer energy quick input`);
  assertIncludes(html, 'renderTodayLifeQuickCard', `${label} first-layer life renderer`);
  assertIncludes(html, 'recordLifeLog', `${label} shared life log recorder`);
  assertIncludes(html, 'saveTodayLifeQuick', `${label} first-layer life save action`);
  assertIncludes(html, 'focusTodayLifeQuick', `${label} first-layer life focus shortcut`);
  assertIncludes(html, '?暑瑼Ｘ', `${label} first-layer life card kicker`);
  assertIncludes(html, 'getDailyCoreSnapshot', `${label} daily core snapshot helper`);
  assertIncludes(html, 'renderDailyCoreCard', `${label} daily core renderer`);
  assertIncludes(html, 'renderDailyCoreMission', `${label} daily core mission renderer`);
  assertIncludes(html, '隞銝餌?嚗?, `${label} daily core mission copy`);
  assertIncludes(html, 'daily-core-mission', `${label} daily core mission style`);
  assertIncludes(html, 'daily-core-after-lock', `${label} daily core same-meal after-photo lock style`);
  assertIncludes(html, 'renderDailyCoreAfterLock', `${label} daily core same-meal after-photo renderer`);
  assertIncludes(html, '????嚗???銝蝑?, `${label} same-meal after-photo first-layer copy`);
  assertIncludes(html, 'daily-core-chip', `${label} daily core status chip style`);
  assertIncludes(html, 'daily-core-progress-fill', `${label} daily core progress style`);
  assertIncludes(html, 'completionText', `${label} daily core completion text`);
  assertIncludes(html, '隞?', `${label} daily score copy`);
  assertIncludes(html, '銝?甇伐?', `${label} daily next-step copy`);
  assertIncludes(html, '隞銝餌?隞餃?', `${label} daily check-in copy`);
  assertIncludes(html, '隞予鈭辣鈭??????????銝?擗?, `${label} first-layer daily routine copy`);
  assertIncludes(html, '? / ?貊??, `${label} daily photo action`);
  assertIncludes(html, 'focusTodayWaterQuick()', `${label} daily water action focuses first-layer card`);
  assertIncludes(html, '隞擃?', `${label} daily weight action`);
  assertIncludes(html, '???噶', `${label} daily life action`);
  assertIncludes(html, '??隞暻?, `${label} daily meal advice action`);
  assertIncludes(html, 'id="photoInput"', `${label} before photo input`);
  assertIncludes(html, 'id="photoAlbumInput"', `${label} before album photo input`);
  assertIncludes(html, 'id="afterPhotoInput"', `${label} after photo input`);
  assertIncludes(html, 'id="afterPhotoAlbumInput"', `${label} after album photo input`);
  assertIncludes(html, 'getPhotoInputIdForMode', `${label} photo picker mode mapping`);
  assertIncludes(html, 'lastPhotoPickerRequest', `${label} photo picker request tracking`);
  assertIncludes(html, 'showPhotoPickerFallback', `${label} photo picker fallback`);
  assertIncludes(html, 'photo-btn-label.awaiting', `${label} photo picker fallback highlight`);
  assertExcludes(html, 'id="bottomPhotoInput"', `${label} bottom photo input overlay guard`);
  assertIncludes(html, '.bottom-nav > .photo-file-input { display: none; }', `${label} bottom nav file input does not cover tabs`);
  assertIncludes(html, 'id="photoSourceSheet"', `${label} bottom photo source sheet`);
  assertIncludes(html, 'id="photoSourceTitle"', `${label} contextual photo source title`);
  assertIncludes(html, 'id="photoSourceBody"', `${label} contextual photo source body`);
  assertIncludes(html, 'renderPhotoSourceSheet', `${label} contextual photo source renderer`);
  assertIncludes(html, 'setPhotoSourceOptionOrder', `${label} after-photo option ordering`);
  assertIncludes(html, 'afterFirst: true', `${label} after-photo options first`);
  assertIncludes(html, 'afterFirst ? 1 : 3', `${label} after-photo top order rule`);
  assertIncludes(html, '憌臬??扳?舫?⊥迤嚗?鋆???擗?, `${label} after-photo source toast copy`);
  assertIncludes(html, '憌臬??扳?摰停???喳?梢???擗?撱箄降', `${label} before-photo immediate estimate copy`);
  assertIncludes(html, '銝??嗆??啁?銝擗?, `${label} after-photo same-meal sheet copy`);
  assertIncludes(html, 'openPhotoSourceSheet', `${label} photo source sheet opener`);
  assertIncludes(html, 'choosePhotoSource', `${label} photo source chooser`);
  assertIncludes(html, "choosePhotoSource('beforeAlbum')", `${label} bottom before album option`);
  assertIncludes(html, "choosePhotoSource('afterAlbum')", `${label} bottom after album option`);
  assertIncludes(html, "openPhotoSourceSheet('before')", `${label} first-layer photo source chooser`);
  assertIncludes(html, '? / ?貊??, `${label} first-layer camera and album copy`);
  assertIncludes(html, 'id="installNudgeCard"', `${label} install nudge card`);
  assertIncludes(html, 'renderInstallNudgeCard', `${label} install nudge renderer`);
  assertIncludes(html, 'dismissInstallNudge', `${label} install nudge dismiss`);
  assertIncludes(html, '??paipachi ?曉??銝餌??, `${label} install nudge copy`);
  assertIncludes(html, 'photo-coach-card', `${label} photo framing coach card`);
  assertIncludes(html, '?港遢?仿', `${label} full meal framing tip`);
  assertIncludes(html, '皝舫ㄡ?脩?蝝?, `${label} soup and drink framing tip`);
  assertIncludes(html, '憌臬???摨?, `${label} after photo same angle tip`);
  assertIncludes(html, 'id="photoPlaceMemoryCard"', `${label} photo tab place memory card`);
  assertIncludes(html, 'renderPhotoPlaceMemoryCard', `${label} photo tab place memory renderer`);
  assertIncludes(html, 'selectPhotoPlaceMemory', `${label} photo tab place memory selector`);
  assertIncludes(html, '?餈?摰塚?銝?萄葆??, `${label} recent place memory CTA`);
  assertIncludes(html, 'photo-place-memory-health', `${label} recent place memory health style`);
  assertIncludes(html, '銝活??鋆?, `${label} recent place memory completion hint`);
  assertIncludes(html, '閮 ${health.healthScore || 0}%', `${label} recent place memory health score`);
  assertIncludes(html, 'id="todayDiarySummary"', `${label} today diary`);
  assertIncludes(html, 'id="todayLifeMemoryCard"', `${label} today life memory card`);
  assertIncludes(html, 'id="personalDietMemoryCard"', `${label} personal diet memory card`);
  assertIncludes(html, 'personal-diet-memory-card', `${label} personal diet memory style`);
  assertIncludes(html, 'getPersonalDietMemoryProfile', `${label} personal diet memory profile helper`);
  assertIncludes(html, 'renderPersonalDietMemoryCard', `${label} personal diet memory renderer`);
  assertIncludes(html, 'getMealDietIssueSignals', `${label} personal recurring issue helper`);
  assertIncludes(html, '?犖憌脤?頛芸?', `${label} personal diet memory copy`);
  assertIncludes(html, '撣詨?', `${label} personal frequent food copy`);
  assertIncludes(html, '撣貉???', `${label} personal recurring issue copy`);
  assertIncludes(html, 'today-life-memory-card', `${label} today life memory style`);
  assertIncludes(html, 'getLifeMemoryLabels', `${label} life memory label helper`);
  assertIncludes(html, 'renderTodayLifeMemoryCard', `${label} life memory renderer`);
  assertIncludes(html, '?暑?????, `${label} life memory copy`);
  assertIncludes(html, 'id="recentMemoryStrip"', `${label} recent memory strip`);
  assertIncludes(html, '?餈???, `${label} recent memory copy`);
  assertIncludes(html, 'id="memorySearchPanel"', `${label} cross-date meal memory search panel`);
  assertIncludes(html, 'renderMemorySearchPanel', `${label} meal memory search renderer`);
  assertIncludes(html, 'searchMealMemories', `${label} meal memory search helper`);
  assertIncludes(html, 'openMemorySearchResult', `${label} meal memory search jump action`);
  assertIncludes(html, '?交??隞暻?, `${label} meal memory search copy`);
  assertIncludes(html, '??擗??????閮?, `${label} meal memory search placeholder`);
  assertIncludes(html, 'id="memoryPhotoTimeline"', `${label} memory photo timeline`);
  assertIncludes(html, 'id="todayPhotoBrief"', `${label} today photo brief`);
  assertIncludes(html, 'renderTodayPhotoBrief', `${label} today photo brief renderer`);
  assertIncludes(html, 'id="todayMealLedger"', `${label} today meal ledger`);
  assertIncludes(html, 'renderTodayMealLedger', `${label} today meal ledger renderer`);
  assertIncludes(html, 'today-meal-ledger-row', `${label} today meal ledger rows`);
  assertIncludes(html, 'today-meal-ledger-actions', `${label} today meal ledger quick actions`);
  assertIncludes(html, '隞?ㄞ敹急', `${label} today meal quick action label`);
  assertIncludes(html, '鋆?餈ㄞ敺?, `${label} today quick after-photo action`);
  assertIncludes(html, 'today-meal-after-action', `${label} per-meal after-photo action style`);
  assertIncludes(html, '鋆?憌臬???, `${label} per-meal after-photo action copy`);
  assertIncludes(html, 'id="todaySavedFollowupCard"', `${label} post-save follow-up card`);
  assertIncludes(html, 'renderTodaySavedFollowup', `${label} post-save follow-up renderer`);
  assertIncludes(html, 'getSavedMealNutritionCoach', `${label} post-save nutrition coach helper`);
  assertIncludes(html, 'id="immediateNutritionCoachCard"', `${label} immediate nutrition coach card`);
  assertIncludes(html, 'renderImmediateNutritionCoachCard', `${label} immediate nutrition coach renderer`);
  assertIncludes(html, 'buildCurrentEstimateMealForCoach', `${label} immediate nutrition coach estimate helper`);
  assertIncludes(html, 'getNutritionStatusForMeals', `${label} projected nutrition status helper`);
  assertIncludes(html, 'startCurrentEstimateRecommendedPlan', `${label} current estimate next-meal planner`);
  assertIncludes(html, 'current_estimate_next', `${label} current estimate plan route`);
  assertIncludes(html, '隡啁?敺?銝擗???, `${label} current estimate plan label`);
  assertIncludes(html, 'projectedMeals', `${label} projected meal next advice state`);
  assertIncludes(html, 'TATA ?單???撣?, `${label} immediate nutrition coach copy`);
  assertIncludes(html, '?券遢撱箄降??銝擗?, `${label} immediate nutrition coach next-meal CTA`);
  assertIncludes(html, 'today-saved-followup', `${label} post-save follow-up style`);
  assertIncludes(html, '憛???撣怠遣霅?, `${label} per-meal nutritionist advice copy`);
  assertIncludes(html, '?祇?閰?', `${label} per-meal nutrition score copy`);
  assertIncludes(html, '?芷?嚗?, `${label} per-meal nutrition strength copy`);
  assertIncludes(html, '銝雲嚗?, `${label} per-meal nutrition gap copy`);
  assertIncludes(html, 'post-meal-decision-btn', `${label} post-save nutrition coach CTA style`);
  assertIncludes(html, '????撌脣?憟?, `${label} post-save follow-up copy`);
  assertIncludes(html, '???啣?銝擗甇??銝?霈??啁???, `${label} post-save same-meal after-photo copy`);
  assertIncludes(html, 'getTodayCalorieBudgetSnapshot', `${label} post-save calorie budget snapshot`);
  assertIncludes(html, 'today-saved-followup-budget', `${label} post-save calorie budget style`);
  assertIncludes(html, '隞???kcal', `${label} post-save calories-left copy`);
  assertIncludes(html, '銝?擗?蝞?, `${label} post-save next meal budget copy`);
  assertIncludes(html, 'renderTodaySavedPlaceMemory', `${label} post-save place memory renderer`);
  assertIncludes(html, 'today-saved-place-memory', `${label} post-save place memory style`);
  assertIncludes(html, '??鋆???銝活?曉???, `${label} post-save place memory CTA copy`);
  assertIncludes(html, '??撌脰???赤閮', `${label} post-save completed place memory copy`);
  assertIncludes(html, 'goToTodayMealMemory', `${label} post-save today memory navigation`);
  assertIncludes(html, 'goToMealMemoryDate', `${label} dated meal memory navigation`);
  assertIncludes(html, 'lastSavedMealFocusId', `${label} last saved meal focus state`);
  assertIncludes(html, 'data-meal-id', `${label} focused meal row marker`);
  assertIncludes(html, '???湔', `${label} focused meal copy`);
  assertIncludes(html, 'id="todayActionTimeline"', `${label} today action timeline`);
  assertIncludes(html, 'renderTodayActionTimeline', `${label} today action timeline renderer`);
  assertIncludes(html, '隞予?ㄞ瘚?', `${label} today action timeline copy`);
  assertIncludes(html, '?ㄞ????, `${label} action timeline before-photo step`);
  assertIncludes(html, '???航???, `${label} action timeline after-photo step`);
  assertIncludes(html, '鋆偌?郊銵?, `${label} action timeline water step`);
  assertIncludes(html, '??銝擗獐??, `${label} action timeline next-meal step`);
  assertIncludes(html, 'getMealDisplayTime', `${label} meal time formatter`);
  assertIncludes(html, 'class="meal-open-launch"', `${label} first-screen 10-second meal launchpad`);
  assertIncludes(html, 'meal-open-launch-btn primary', `${label} launchpad primary camera action`);
  assertIncludes(html, "openPhotoPicker('beforeAlbum')", `${label} launchpad album picker action`);
  assertIncludes(html, '10 蝘?憌臬??, `${label} launchpad accessibility copy`);
  assertIncludes(html, 'id="mealPlaceName"', `${label} meal place name input`);
  assertIncludes(html, 'editMealPlaceMemory', `${label} meal place memory quick editor`);
  assertIncludes(html, 'id="placeQuickEditPanel"', `${label} inline place memory editor panel`);
  assertIncludes(html, 'openPlaceQuickEdit', `${label} inline place memory opener`);
  assertIncludes(html, 'savePlaceQuickEdit', `${label} inline place memory saver`);
  assertIncludes(html, 'activePlaceQuickEdit', `${label} inline place memory active state`);
  assertIncludes(html, 'dateKey: selectedDate', `${label} meal ledger receives selected memory date`);
  assertIncludes(html, "editMealPlaceMemory('${mealId}', '${ledgerDateKey}', event)", `${label} historical place memory edit action`);
  assertIncludes(html, 'updateMealPlaceMemory', `${label} meal place memory updater`);
  assertIncludes(html, 'today-meal-place-action', `${label} meal place memory action style`);
  assertIncludes(html, 'openMealPlaceMap', `${label} meal place map opener`);
  assertIncludes(html, 'selectMealRevisitPlan', `${label} meal row revisit plan selector`);
  assertIncludes(html, 'today-meal-place-action map', `${label} meal place map action style`);
  assertIncludes(html, 'today-meal-place-action revisit', `${label} meal row revisit action style`);
  assertIncludes(html, '???, `${label} meal place map action copy`);
  assertIncludes(html, '?折???', `${label} meal row revisit action copy`);
  assertIncludes(html, '鋆???閰?', `${label} meal place quick edit copy`);
  assertIncludes(html, '擗輒?暺???, `${label} place memory copy`);
  assertIncludes(html, 'getMealPlaceMemory', `${label} place memory collector`);
  assertIncludes(html, 'captureMealLocationSnapshot', `${label} automatic meal location snapshot`);
  assertIncludes(html, 'activeMealLocationSnapshot', `${label} active meal location state`);
  assertIncludes(html, 'locationLatitude', `${label} meal location latitude persistence`);
  assertIncludes(html, 'browser_geolocation', `${label} browser geolocation location source`);
  assertIncludes(html, 'formatPlaceMemory', `${label} place memory formatter`);
  assertIncludes(html, 'id="placeRevisitList"', `${label} place revisit list`);
  assertIncludes(html, 'getPlaceSearchText', `${label} place revisit full-memory search helper`);
  assertIncludes(html, 'parsePlaceSearchIntent', `${label} natural language place search parser`);
  assertIncludes(html, 'doesPlaceMatchSearchIntent', `${label} natural language place search matcher`);
  assertIncludes(html, 'getPlaceSearchIntentSummary', `${label} natural language place search summary`);
  assertIncludes(html, 'getPlaceSearchMatches', `${label} place revisit matched meal renderer`);
  assertIncludes(html, '?餃僑憯賢', `${label} place memory natural search sushi example`);
  assertIncludes(html, '?唬葉??', `${label} place memory natural search steak example`);
  assertIncludes(html, '閬芸???', `${label} place memory natural search family example`);
  assertIncludes(html, '擃???, `${label} place memory natural search protein example`);
  assertIncludes(html, '?抒?', `${label} place memory natural search sugar-control example`);
  assertIncludes(html, '??摨???暺??閮?, `${label} place revisit food/date search placeholder`);
  assertIncludes(html, '???賭葉嚗?, `${label} place revisit matched meal copy`);
  assertIncludes(html, 'id="placeMemoryHealthCard"', `${label} place memory health card`);
  assertIncludes(html, 'getPlaceMemoryHealth', `${label} place memory health helper`);
  assertIncludes(html, 'renderPlaceMemoryHealthCard', `${label} place memory health renderer`);
  assertIncludes(html, 'place-memory-health-card', `${label} place memory health style`);
  assertIncludes(html, '摨振閮?亙熒摨?, `${label} place memory health copy`);
  assertIncludes(html, 'id="placeMemoryPassportCard"', `${label} place memory passport card`);
  assertIncludes(html, 'getPlaceMemoryPassport', `${label} place memory passport helper`);
  assertIncludes(html, 'renderPlaceMemoryPassportCard', `${label} place memory passport renderer`);
  assertIncludes(html, 'place-memory-passport-card', `${label} place memory passport style`);
  assertIncludes(html, '?犖憌脤??啣?霅瑞', `${label} place memory passport copy`);
  assertIncludes(html, '閮 ${health.healthScore}%', `${label} place revisit health score tag`);
  assertIncludes(html, '擗輒?赤皜', `${label} place revisit copy`);
  assertIncludes(html, 'getPlaceRevisitSummaries', `${label} place revisit summary helper`);
  assertIncludes(html, 'renderPlaceRevisitList', `${label} place revisit renderer`);
  assertIncludes(html, 'id="placeRevisitSearch"', `${label} place revisit search input`);
  assertIncludes(html, 'handlePlaceRevisitSearch', `${label} place revisit search handler`);
  assertIncludes(html, 'placeRevisitCategory', `${label} place category filter state`);
  assertIncludes(html, 'getPlaceDietCategoryOptions', `${label} place category options helper`);
  assertIncludes(html, 'getPlaceDietCategories', `${label} place diet category inference`);
  assertIncludes(html, 'getPlaceNutritionDataset', `${label} place nutrition dataset helper`);
  assertIncludes(html, 'renderPlaceNutritionDataset', `${label} place nutrition dataset renderer`);
  assertIncludes(html, 'place-nutrition-dataset', `${label} place nutrition dataset style`);
  assertIncludes(html, 'getPlaceGoalFitScore', `${label} place goal fit score helper`);
  assertIncludes(html, 'renderPlaceGoalFit', `${label} place goal fit renderer`);
  assertIncludes(html, 'place-goal-fit', `${label} place goal fit style`);
  assertIncludes(html, 'placeRevisitGoalFitOnly', `${label} place goal fit filter state`);
  assertIncludes(html, 'togglePlaceRevisitGoalFitOnly', `${label} place goal fit filter action`);
  assertIncludes(html, '?格??拙?', `${label} place goal fit filter copy`);
  assertIncludes(html, 'getPlaceGoalFitScore(place).score < 68', `${label} place goal fit threshold`);
  assertIncludes(html, '靘?{getGoalLabel()}??', `${label} place goal fit sort meta`);
  assertIncludes(html, '靘犖?格??斗摨振?拚?摨?, `${label} place goal fit aria label`);
  assertIncludes(html, '?拚?嚗?, `${label} place goal fit copy`);
  assertIncludes(html, '摨振?祕憌脤??豢?', `${label} place nutrition dataset aria label`);
  assertIncludes(html, '撟喳? kcal', `${label} place nutrition calories metric`);
  assertIncludes(html, 'setPlaceRevisitCategory', `${label} place category filter action`);
  assertIncludes(html, 'place-revisit-category-row', `${label} place category chip style`);
  assertIncludes(html, '皜???', `${label} slimming-friendly place category`);
  assertIncludes(html, '擃???, `${label} high-protein place category`);
  assertIncludes(html, '閬芸???', `${label} family-friendly place category`);
  assertIncludes(html, '?抒???', `${label} sugar-control place category`);
  assertIncludes(html, 'togglePlaceRevisitHighOnly', `${label} high rating place filter`);
  assertIncludes(html, 'placeRevisitIncompleteOnly', `${label} incomplete place memory filter state`);
  assertIncludes(html, 'togglePlaceRevisitIncompleteOnly', `${label} incomplete place memory filter`);
  assertIncludes(html, '敺?鞈?', `${label} incomplete place memory filter copy`);
  assertIncludes(html, '??鋆?摰?, `${label} place health incomplete CTA`);
  assertIncludes(html, 'getPlaceRevisitAdvice', `${label} place revisit advice helper`);
  assertIncludes(html, 'getPlaceMemoryInsight', `${label} place memory insight helper`);
  assertIncludes(html, 'renderPlaceMemoryInsight', `${label} place memory insight renderer`);
  assertIncludes(html, 'place-memory-insight', `${label} place memory insight style`);
  assertIncludes(html, 'buildPlacePersonalGuide', `${label} personal place guide helper`);
  assertIncludes(html, 'renderPlacePersonalGuide', `${label} personal place guide renderer`);
  assertIncludes(html, 'place-personal-guide', `${label} personal place guide style`);
  assertIncludes(html, '?犖摨振?餌', `${label} personal place guide copy`);
  assertIncludes(html, '隞暻潭??', `${label} place guide timing copy`);
  assertIncludes(html, '銝活?獐暺?, `${label} place guide order copy`);
  assertIncludes(html, '撱箇??赤暺?', `${label} place guide revisit CTA`);
  assertIncludes(html, '摨振?赤瘣?', `${label} place memory insight copy`);
  assertIncludes(html, '?雿喳???, `${label} place memory best memory copy`);
  assertIncludes(html, '憸券??', `${label} place memory risk copy`);
  assertIncludes(html, '銝活暺?', `${label} place memory next order copy`);
  assertIncludes(html, 'buildPlaceMapsUrl', `${label} place map URL helper`);
  assertIncludes(html, 'selectPlaceRevisitPlan', `${label} place revisit plan selector`);
  assertIncludes(html, 'place-revisit-action primary', `${label} place revisit plan CTA`);
  assertIncludes(html, 'id="placeMemoryDetail"', `${label} place memory detail panel`);
  assertIncludes(html, 'openPlaceMemoryDetail', `${label} place memory detail opener`);
  assertIncludes(html, 'renderPlaceMemoryChecklist', `${label} place memory checklist renderer`);
  assertIncludes(html, 'place-memory-checklist', `${label} place memory checklist style`);
  assertIncludes(html, '鋆??振摨?閮', `${label} place memory checklist copy`);
  assertIncludes(html, '撱箇??赤閮', `${label} place memory checklist plan CTA`);
  assertIncludes(html, 'closePlaceMemoryDetail', `${label} place memory detail close`);
  assertIncludes(html, '????暻?, `${label} place memory detail CTA`);
  assertIncludes(html, 'renderPostMealPlaceNudge', `${label} post-meal place nudge renderer`);
  assertIncludes(html, '??鋆???銝活?曉???, `${label} post-meal place nudge copy`);
  assertIncludes(html, 'quickRateMealPlace', `${label} post-meal quick place rating helper`);
  assertIncludes(html, '敹恍???', `${label} quick place rating note`);
  assertIncludes(html, '?喳???, `${label} quick positive revisit rating`);
  assertIncludes(html, '銝???, `${label} quick negative revisit rating`);
  assertIncludes(html, "const has = (key) => Object.prototype.hasOwnProperty.call(patch, key);", `${label} place memory patch preserves existing fields`);
  assertIncludes(html, 'promptPlaceMemoryAfterSave', `${label} post-save place memory prompt`);
  assertIncludes(html, '????撌脣?憟?, `${label} post-save place memory hint`);
  assertIncludes(html, '?ㄞ?? paipachi ????, `${label} meal habit memory copy`);
  assertIncludes(html, 'id="mealOpenHabitCard"', `${label} meal open habit card`);
  assertIncludes(html, 'renderMealOpenHabitCard', `${label} meal open habit renderer`);
  assertIncludes(html, 'getNextMealSlotForHabit', `${label} meal habit slot helper`);
  assertIncludes(html, 'getMealOpenTimeNudge', `${label} meal-time nudge helper`);
  assertIncludes(html, 'meal-open-nudge', `${label} meal-time nudge style`);
  assertIncludes(html, 'meal-open-nudge-actions', `${label} meal-time nudge action layout`);
  assertIncludes(html, 'pendingAfterItems', `${label} meal open pending after-photo state`);
  assertIncludes(html, '隞予??${pendingAfterItems.length} 擗?鋆ㄞ敺', `${label} meal open pending after-photo title`);
  assertIncludes(html, '??憌臬??扯????祇銝擗?, `${label} meal open same-meal after-photo copy`);
  assertIncludes(html, 'primaryMainLabel', `${label} meal open synchronized primary action`);
  assertIncludes(html, '?曉??{currentSlot}??', `${label} current meal-time nudge copy`);
  assertIncludes(html, '鋆?撘菜皞?, `${label} after-photo nudge copy`);
  assertIncludes(html, 'startMissedMealRecovery', `${label} missed before-photo recovery helper`);
  assertIncludes(html, '??摰?閮?, `${label} missed before-photo recovery CTA`);
  assertIncludes(html, "openPhotoPicker('after')", `${label} missed meal recovery opens after photo`);
  assertIncludes(html, '?ㄞ撠望???, `${label} meal opening habit copy`);
  assertIncludes(html, '銝??隞暻?, `${label} decision helper CTA`);
  assertIncludes(html, 'id="coachAnswerStatus"', `${label} coach answer status card`);
  assertIncludes(html, 'renderCoachAnswerStatus', `${label} coach answer status renderer`);
  assertIncludes(html, 'openMealDecisionCoach', `${label} reliable meal decision helper`);
  assertIncludes(html, "openMealDecisionCoach('蝑???暻?)", `${label} no-decision CTA uses reliable helper`);
  assertIncludes(html, 'selectTataDecisionAndOpen', `${label} one-tap TATA decision to photo`);
  assertIncludes(html, '憛??湔撟急???, `${label} one-tap decision copy`);
  assertExcludes(html, "setTimeout(() => askTataCoach(text), 30)", `${label} old double-render decision path`);
  assertIncludes(html, 'focusTataCoachAnswer(preferredId', `${label} coach answer stable focus helper`);
  assertIncludes(html, '憛?甇???, `${label} coach answer visible feedback copy`);
  assertIncludes(html, 'renderMealDecisionPathCard(status, advice)', `${label} coach refreshes decision paths`);
  assertIncludes(html, 'quickStartFirstMeal', `${label} quick-start first meal helper`);
  assertIncludes(html, '??蝚砌?擗?鞈?銋?鋆?, `${label} quick-start first meal CTA`);
  assertIncludes(html, '頨恍?擃?銋??臬?澈擃隅?Ｕ矽??, `${label} deferred body profile copy`);
  assertIncludes(html, 'id="onboardHeightInput"', `${label} direct height input`);
  assertIncludes(html, 'id="onboardWeightInput"', `${label} direct weight input`);
  assertIncludes(html, 'syncOnboardDirectInput', `${label} direct body input sync`);
  assertIncludes(html, '?湔頛詨頨恍? cm', `${label} direct height placeholder`);
  assertIncludes(html, '?湔頛詨擃? kg', `${label} direct weight placeholder`);
  assertIncludes(html, '隞?抒?蝮質汗', `${label} today photo brief copy`);
  assertIncludes(html, '??瘥?', `${label} photo compare summary`);
  assertIncludes(html, '敺???, `${label} after photo pending summary`);
  assertIncludes(html, 'startLatestAfterPhoto', `${label} latest after photo helper`);
  assertIncludes(html, 'renderMemoryPhotoTimeline', `${label} memory photo timeline renderer`);
  assertIncludes(html, '?抒??瘚?, `${label} memory photo timeline copy`);
  assertIncludes(html, 'memory-photo-card', `${label} memory photo card style`);
  assertIncludes(html, '憌臬?靽?', `${label} after photo memory badge`);
  assertIncludes(html, 'fetchRemoteMealDates', `${label} recent memory dates fetcher`);
  assertIncludes(html, 'openRecentMemoryDate', `${label} recent memory date opener`);
  assertIncludes(html, 'id="todayRecapCard"', `${label} today recap card`);
  assertIncludes(html, 'id="todayDecisionBrief"', `${label} today decision brief`);
  assertIncludes(html, 'id="todayRouteCard"', `${label} today three-step route card`);
  assertIncludes(html, 'renderTodayRouteCard', `${label} today three-step route renderer`);
  assertIncludes(html, 'id="todayReturnMissionCard"', `${label} today return mission card`);
  assertIncludes(html, 'getTodayReturnMission', `${label} today return mission helper`);
  assertIncludes(html, 'renderTodayReturnMissionCard', `${label} today return mission renderer`);
  assertIncludes(html, 'today-return-mission-card', `${label} today return mission style`);
  assertIncludes(html, 'getTodayOpenValueItems', `${label} daily open value helper`);
  assertIncludes(html, '隞予?? paipachi ???唬?暻?, `${label} daily open value accessibility label`);
  assertIncludes(html, '?抒??', `${label} daily open memory value`);
  assertIncludes(html, '?格??批', `${label} daily calorie-control value`);
  assertIncludes(html, '摨振閮', `${label} daily place-memory value`);
  assertIncludes(html, 'id="mealCountdownCard"', `${label} meal countdown card`);
  assertIncludes(html, 'getNextMealWindowState', `${label} meal countdown window helper`);
  assertIncludes(html, 'renderMealCountdownCard', `${label} meal countdown renderer`);
  assertIncludes(html, 'meal-countdown-card', `${label} meal countdown style`);
  assertIncludes(html, '?ㄞ?', `${label} meal countdown copy`);
  assertIncludes(html, '??憌臬???, `${label} meal countdown before-photo CTA`);
  assertIncludes(html, '隞予????暻?, `${label} today return mission copy`);
  assertIncludes(html, '銝?甇亙??隞嗡?撠勗末', `${label} one-action return mission copy`);
  assertIncludes(html, '隞予銝郊嚗?Ｖ?', `${label} today route title copy`);
  assertIncludes(html, '?ㄞ????, `${label} today route before-photo step`);
  assertIncludes(html, '???⊥迤??瘞?, `${label} today route recovery step`);
  assertIncludes(html, '???塚???銝擗?, `${label} today route memory step`);
  assertIncludes(html, 'buildDailyShareText', `${label} daily share text helper`);
  assertIncludes(html, 'shareDailyRecap', `${label} daily recap share helper`);
  assertIncludes(html, 'today-recap-actions', `${label} daily recap action layout`);
  assertIncludes(html, '?澈隞?', `${label} daily recap share CTA`);
  assertIncludes(html, 'renderTodayDecisionBrief', `${label} today decision renderer`);
  assertIncludes(html, 'startTodayRecommendedMealPlan', `${label} one-tap today recommendation helper`);
  assertIncludes(html, 'today-recommendation-row', `${label} today recommendation row`);
  assertIncludes(html, '?批遣霅啣?', `${label} one-tap today recommendation CTA`);
  assertIncludes(html, 'route: "today_recommendation"', `${label} today recommendation meal plan route`);
  assertIncludes(html, '??臬?憌舀??, `${label} recommendation preserves photo-based portion correction`);
  assertIncludes(html, '隞瘙箇?', `${label} today decision copy`);
  assertIncludes(html, '?迤?梢?隞誑憌臬??批?隞賡??⊥迤', `${label} photo-first decision copy`);
  assert.ok(
    html.indexOf('id="todayDecisionBrief"') < html.indexOf('class="hero-section"'),
    `${label} today decision should render before hero`
  );
  assertIncludes(html, '<details class="tata-chapter-panel">', `${label} collapsible TATA story chapters`);
  assertIncludes(html, '隞蝮賜?', `${label} today recap copy`);
  assertIncludes(html, 'renderTodayRecapCard', `${label} today recap renderer`);
  assertIncludes(html, 'id="waterNeedText"', `${label} water tracker`);
  assertIncludes(html, 'id="waterRhythmCard"', `${label} water rhythm card`);
  assertIncludes(html, 'id="waterReminderCard"', `${label} water reminder card`);
  assertIncludes(html, 'getWaterReminderPlan', `${label} water reminder planner`);
  assertIncludes(html, 'renderWaterReminderCard', `${label} water reminder renderer`);
  assertIncludes(html, 'water-reminder-grid', `${label} water reminder stat grid`);
  assertIncludes(html, '憛??偌蝭憟?, `${label} water rhythm copy`);
  assertIncludes(html, 'renderWaterRhythm', `${label} water rhythm renderer`);
  assertIncludes(html, 'id="stepCounterStatus"', `${label} auto step status`);
  assertIncludes(html, 'id="stepAutoModePill"', `${label} auto step mode pill`);
  assertIncludes(html, 'APP ????摯甇?, `${label} honest web step tracking copy`);
  assertIncludes(html, '摰??郊閬????亙熒鞈?銝脫', `${label} native background step boundary copy`);
  assertIncludes(html, 'Web ????APP ????摯甇?, `${label} auto step web-open copy`);
  assertIncludes(html, 'step-auto-note-grid', `${label} auto step explanation chips`);
  assertExcludes(html, '??芸?閮郊', `${label} no manual step enable wording`);
  assertIncludes(html, '隞?芸?甇交', `${label} auto step title`);
  assertIncludes(html, 'setStepCounterStatus', `${label} auto step status helper`);
  assertIncludes(html, 'bindAutomaticStepRetryEvents', `${label} auto step retry events`);
  assertIncludes(html, "document.addEventListener('visibilitychange'", `${label} auto step resumes after background`);
  assertIncludes(html, "window.addEventListener('pageshow'", `${label} auto step retries on pageshow`);
  assertIncludes(html, '?芸?隡唳郊敺?璈?甈?, `${label} iOS motion permission copy is automatic`);
  assertIncludes(html, 'id="profileSyncCard"', `${label} profile sync card`);
  assertIncludes(html, 'id="heightDirectInput"', `${label} direct height input`);
  assertIncludes(html, 'id="weightDirectInput"', `${label} direct weight input`);
  assertIncludes(html, 'setTodayWeightFromInput', `${label} direct daily weight setter`);
  assertIncludes(html, 'openBodyQuickLog', `${label} daily weight shortcut`);
  assertIncludes(html, 'id="lifeQuickLogCard"', `${label} life quick log card`);
  assertIncludes(html, 'id="sleepHoursInput"', `${label} sleep input`);
  assertIncludes(html, 'id="bowelStateInput"', `${label} bowel state input`);
  assertIncludes(html, 'id="energyStateInput"', `${label} energy state input`);
  assertIncludes(html, 'saveLifeQuickLog', `${label} life quick log saver`);
  assertIncludes(html, 'renderLifeQuickLogSummary', `${label} life quick log summary renderer`);
  assertIncludes(html, 'getLifeWellnessAdvice', `${label} life wellness advice helper`);
  assertIncludes(html, '?暑???', `${label} life wellness advice copy`);
  assertIncludes(html, '銝?擗?蝥雁?偌鋆?', `${label} bowel-based meal advice`);
  assertIncludes(html, '隞予?仿??ㄡ蝖祆?', `${label} sleep-based meal advice`);
  assertIncludes(html, '撣唾?擃?撌脖?摮?, `${label} profile sync copy`);
  assertIncludes(html, 'renderProfileSyncCard', `${label} profile sync renderer`);
  assertIncludes(html, 'getAccountProfileSnapshot', `${label} account profile snapshot helper`);
  assertIncludes(html, 'profileVersion: 2', `${label} account profile version`);
  assertIncludes(html, 'accountWeightKg', `${label} account baseline weight field`);
  assertIncludes(html, '閮餃??箸?擃?', `${label} account baseline weight copy`);
  assertIncludes(html, '隞擃??芰?隞予', `${label} daily weight separation copy`);
  assertIncludes(html, 'Daily state is a dated body log, not the account baseline profile.', `${label} daily weight should not reset profile weight`);
  assertExcludes(html, 'userData.currentWeight = Number(state.weightKg)', `${label} daily state must not overwrite account profile weight`);
  assertIncludes(html, 'id="dataHealthCard"', `${label} data health card`);
  assertIncludes(html, '鞈??亙熒瑼Ｘ', `${label} data health copy`);
  assertIncludes(html, 'getDataHealthSnapshot', `${label} data health snapshot helper`);
  assertIncludes(html, 'renderDataHealthCard', `${label} data health renderer`);
  assertIncludes(html, 'syncDataHealthNow', `${label} data health sync action`);
  assertIncludes(html, 'refreshDataHealthNow', `${label} data health refresh action`);
  assertIncludes(html, '隞擗???歇蝬?, `${label} date-bound meal photo copy`);
  assertIncludes(html, 'id="bodyRhythmCard"', `${label} body rhythm card`);
  assertIncludes(html, 'body-rhythm-card', `${label} body rhythm style`);
  assertIncludes(html, 'renderBodyRhythmCard', `${label} body rhythm renderer`);
  assertIncludes(html, 'getSevenDayWeightTrend', `${label} body rhythm trend helper`);
  assertIncludes(html, '頨恍?蝭憟?, `${label} body rhythm kicker`);
  assertIncludes(html, 'stepBurn', `${label} body rhythm step burn`);
  assertIncludes(html, 'calorieBalance', `${label} body rhythm calorie balance`);
  assertIncludes(html, 'id="estimateTrustLine"', `${label} estimate trust line`);
  assertIncludes(html, '隡啁??臭縑摨?, `${label} estimate trust copy`);
  assertIncludes(html, 'class="estimate-advanced-details"', `${label} simplified estimate advanced details`);
  assertIncludes(html, '?脤?蝝啁??耨甇?, `${label} estimate advanced details copy`);
  assertIncludes(html, 'estimate-advanced-content', `${label} estimate advanced details content`);
  assertIncludes(html, 'id="estimateExplainCard"', `${label} estimate explain card`);
  assertIncludes(html, 'id="estimateCalibrationCard"', `${label} estimate calibration card`);
  assertIncludes(html, 'getEstimateCalibrationCoach', `${label} estimate calibration coach helper`);
  assertIncludes(html, 'renderEstimateCalibrationCard', `${label} estimate calibration renderer`);
  assertIncludes(html, 'estimate-calibration-card', `${label} estimate calibration style`);
  assertIncludes(html, '憛?隡啁??⊥迤', `${label} estimate calibration copy`);
  assertIncludes(html, '?閰脩Ⅱ隤?, `${label} estimate calibration missing-info copy`);
  assertIncludes(html, '皝舀?/憌脫???, `${label} estimate calibration soup drink cue`);
  assertIncludes(html, '?祆活隡啁?靘?', `${label} estimate explain copy`);
  assertIncludes(html, 'setEstimateLoadingState', `${label} instant photo loading helper`);
  assertIncludes(html, 'showPhotoReceivedState', `${label} immediate photo received helper`);
  assertIncludes(html, 'showQuickPhotoFallback', `${label} quick photo fallback helper`);
  assertIncludes(html, 'local_photo_quick_fallback', `${label} slow AI quick local fallback source`);
  assertIncludes(html, 'scrollEstimateResultIntoView', `${label} photo result auto-scroll helper`);
  assertIncludes(html, 'getAutoMealRecordDraftKey', `${label} auto meal record draft key`);
  assertIncludes(html, 'saveAutoMealRecordDraft', `${label} auto meal record draft saver`);
  assertIncludes(html, 'nutritionCoach: coach', `${label} nutrition coach should be persisted in auto draft`);
  assertIncludes(html, 'recognitionSummary', `${label} recognition summary should be persisted in auto draft`);
  assertIncludes(html, '<div id="foodRecognitionSummaryCard" class="food-recognition-summary-card"', `${label} visible recognition summary card`);
  assert.ok(
    html.indexOf('id="foodRecognitionSummaryCard"') < html.indexOf('<details class="estimate-advanced-details"'),
    `${label} recognition summary should be visible before advanced details`
  );
  assertIncludes(html, 'retryCurrentPhotoAnalysis', `${label} photo analysis retry helper`);
  assertIncludes(html, 'estimate-retry-row', `${label} photo analysis retry style`);
  assertIncludes(html, '?颲刻??抒?', `${label} photo analysis retry copy`);
  assertIncludes(html, '?券???隡?, `${label} text-assisted retry copy`);
  assertIncludes(html, '?抒?撌脰???, `${label} instant photo read copy`);
  assertIncludes(html, 'AI 颲刻?銝?, `${label} slow AI status copy`);
  assertIncludes(html, '銝?霈??憭?, `${label} stable photo fallback copy`);
  assertIncludes(html, '5800', `${label} faster client AI timeout`);
  assertIncludes(html, 'getEstimateSourceLabel', `${label} estimate source helper`);
  assertIncludes(html, 'id="postMealActionCard"', `${label} post-meal action card`);
  assertIncludes(html, 'renderPostMealDecisionBridge', `${label} post-meal decision bridge renderer`);
  assertIncludes(html, 'startPostMealRecommendedPlan', `${label} post-meal next plan action`);
  assertIncludes(html, '撟急???銝擗?蝑??湔??, `${label} post-save next meal plan CTA`);
  assertIncludes(html, 'post-meal-decision-bridge', `${label} post-meal decision bridge style`);
  assertIncludes(html, '????嚗?銝擗??末', `${label} post-meal next decision copy`);
  assertIncludes(html, '撟急???銝擗?蝑??湔??, `${label} post-meal next plan CTA`);
  assertIncludes(html, 'context.route || "post_meal_next"', `${label} post-meal next plan default route`);
  assertIncludes(html, '蝚砌?擗????予?蝪∪', `${label} first meal retention title`);
  assertIncludes(html, '?予銝??憭?嚗??洵銝擗?, `${label} first meal retention copy`);
  assertIncludes(html, '??鋆??湔?', `${label} first meal after-photo CTA`);
  assertIncludes(html, '??銝擗獐??, `${label} first meal next advice CTA`);
  assertIncludes(html, 'buildMealShareText', `${label} meal share text helper`);
  assertIncludes(html, 'shareMealRecap', `${label} meal share action helper`);
  assertIncludes(html, 'navigator.share', `${label} native share support`);
  assertIncludes(html, 'navigator.clipboard.writeText', `${label} clipboard share fallback`);
  assertIncludes(html, '?澈蝚砌?擗?, `${label} first meal share CTA`);
  assertIncludes(html, 'post-meal-share-actions', `${label} post meal share action layout`);
  assertIncludes(html, 'id="tomorrowMealPromiseCard"', `${label} tomorrow meal promise card`);
  assertIncludes(html, 'buildTomorrowPromiseFocus', `${label} nutrition-aware tomorrow promise helper`);
  assertIncludes(html, 'setTomorrowFirstMealPromise', `${label} tomorrow first meal promise setter`);
  assertIncludes(html, 'completeTomorrowMealPromiseIfDue', `${label} tomorrow promise completion helper`);
  assertIncludes(html, 'lastPromiseWin', `${label} tomorrow promise win persistence`);
  assertIncludes(html, '?典予蝝末?洵銝擗歇摰?', `${label} tomorrow promise completion copy`);
  assertIncludes(html, '雿???摰??典予蝝末?洵銝擗?, `${label} TATA remembers returned first meal copy`);
  assertIncludes(html, '???予蝚砌?擗?, `${label} re-promise tomorrow CTA`);
  assertIncludes(html, 'renderTomorrowMealPromiseCard', `${label} tomorrow first meal promise renderer`);
  assertIncludes(html, 'id="mealStreakCard"', `${label} meal photo streak card`);
  assertIncludes(html, 'renderMealStreakCard', `${label} meal photo streak renderer`);
  assertIncludes(html, 'getMealPhotoStreak', `${label} meal-only streak helper`);
  assertIncludes(html, 'getMealStreakWeek', `${label} seven-day meal streak helper`);
  assertIncludes(html, 'pendingAfterCount', `${label} smart streak after-photo state`);
  assertIncludes(html, 'meal-streak-milestone', `${label} meal streak milestone style`);
  assertIncludes(html, '蝧??蝣?, `${label} meal streak milestone copy`);
  assertIncludes(html, "pendingAfterCount ? '??鋆ㄞ敺鋆???擗? : '??憛?銝擗?蝝末?予蝚砌?擗?", `${label} smart streak next action copy`);
  assertIncludes(html, '隞予?榆蝚砌?撘菟ㄞ?', `${label} first meal streak copy`);
  assertIncludes(html, '?祇?${weekMeals} 擗?, `${label} weekly meal memory summary`);
  assertIncludes(html, "openRecentMemoryDate('${day.date}')", `${label} streak day opens memory date`);
  assertIncludes(html, '蝝?憭拍洵銝擗?, `${label} tomorrow first meal promise CTA`);
  assertIncludes(html, '憛?閮?嚗?憭拍洵銝擗???, `${label} tomorrow promise due copy`);
  assertIncludes(html, '?予蝚砌?擗遙??, `${label} tomorrow nutrition mission copy`);
  assertIncludes(html, '憛?撱箄降嚗?, `${label} tomorrow promise recommendation copy`);
  assertIncludes(html, '?典予蝝末?遙??, `${label} tomorrow promise carryover copy`);
  assertIncludes(html, '??銋?', `${label} post-meal action copy`);
  assertIncludes(html, 'id="tataStateNote"', `${label} TATA state reason surface`);
  assertIncludes(html, 'composeTataStateNote', `${label} TATA state reason helper`);
  assertIncludes(html, '??嚗?, `${label} TATA state reason copy`);
  assertIncludes(html, '撱箄降嚗?, `${label} TATA state action copy`);
  assertIncludes(html, 'id="tataAppearancePanel"', `${label} TATA appearance panel`);
  assertIncludes(html, 'renderTataAppearancePanel', `${label} TATA appearance renderer`);
  assertIncludes(html, '瞍漁?澆?', `${label} TATA beauty state copy`);
  assertIncludes(html, '??皛輯?', `${label} TATA heavy state copy`);
  assertIncludes(html, 'id="tataGrowthCoachCard"', `${label} TATA growth coach card`);
  assertIncludes(html, '憛?隞??勗?', `${label} TATA growth report copy`);
  assertIncludes(html, 'getTataGrowthDrivers', `${label} TATA growth driver helper`);
  assertIncludes(html, 'id="tataFeedCard"', `${label} TATA feeding feedback card`);
  assertIncludes(html, '隞予擗萇策憛????, `${label} TATA feeding feedback copy`);
  assertIncludes(html, 'getTataFeedingFeedback', `${label} TATA feeding feedback helper`);
  assertIncludes(html, 'renderTataFeedCard', `${label} TATA feeding feedback renderer`);
  assertIncludes(html, 'id="tataShareCard"', `${label} TATA status share card`);
  assertIncludes(html, 'getTataShareSnapshot', `${label} TATA status share snapshot helper`);
  assertIncludes(html, 'buildTataShareText', `${label} TATA status share text helper`);
  assertIncludes(html, 'shareTataStatus', `${label} TATA status share action`);
  assertIncludes(html, 'renderTataShareCard', `${label} TATA status share renderer`);
  assertIncludes(html, '憛?隞??', `${label} TATA status share card copy`);
  assertIncludes(html, '?澈憛????, `${label} TATA status share CTA`);
  assertIncludes(html, '瞍漁??, `${label} TATA beauty score copy`);
  assertIncludes(html, '憭???', `${label} TATA appearance warning copy`);
  assertIncludes(html, 'id="tataLevelQuestCard"', `${label} TATA level quest card`);
  assertIncludes(html, '憛???隞餃?', `${label} TATA level quest copy`);
  assertIncludes(html, 'getTataLevelQuest', `${label} TATA level quest helper`);
  assertIncludes(html, 'renderTataLevelQuest', `${label} TATA level quest renderer`);
  assertIncludes(html, 'tata-quest-source-grid', `${label} TATA quest source grid`);
  assertIncludes(html, 'id="tomorrowGrowthPlanCard"', `${label} tomorrow growth plan card`);
  assertIncludes(html, 'getTomorrowGrowthPlan', `${label} tomorrow growth plan helper`);
  assertIncludes(html, 'renderTomorrowGrowthPlan', `${label} tomorrow growth plan renderer`);
  assertIncludes(html, '?霈?憛?餈?, `${label} tomorrow growth plan copy`);
  assertIncludes(html, 'tomorrow-growth-card', `${label} tomorrow growth plan style`);
  assertIncludes(html, '憛???蝡?', `${label} TATA story chapter copy`);
  assertIncludes(html, 'renderTataStoryChapters', `${label} TATA story chapter renderer`);
  assertIncludes(html, 'tata-chapter-list', `${label} TATA story chapter style`);
  assertIncludes(html, 'onclick="switchToTrend()"', `${label} trend shortcut`);
  assertIncludes(html, 'startAfterPhotoForMeal', `${label} after-meal photo shortcut`);
  assertIncludes(html, 'resolveAfterPhotoTargetMeal', `${label} after photo target resolver`);
  assertIncludes(html, 'attachAfterPhotoToRecentMeal', `${label} after photo rescue updater`);
  assertIncludes(html, 'activePhotoSessionId', `${label} photo session guard`);
  assertIncludes(html, 'photoSessionId !== activePhotoSessionId', `${label} stale photo result guard`);
  assertIncludes(html, 'activePhotoDraft', `${label} active photo draft state`);
  assertIncludes(html, 'getActivePhotoDraft', `${label} photo draft reader`);
  assertIncludes(html, 'saveActivePhotoDraft', `${label} photo draft saver`);
  assertIncludes(html, 'restoreActivePhotoDraft', `${label} photo draft restore action`);
  assertIncludes(html, 'clearActivePhotoDraft', `${label} photo draft clear action`);
  assertIncludes(html, "dailyKey('activePhotoDraft')", `${label} daily photo draft key`);
  assertIncludes(html, '?Ｗ儔???抒?', `${label} photo draft restore copy`);
  assertIncludes(html, 'saveActivePhotoDraft({ phase: capturePhase, photo: compressed', `${label} captured photo draft save`);
  assertIncludes(html, 'clearActivePhotoDraft();', `${label} saved meal clears photo draft`);
  assertIncludes(html, '?銝?撘萇??蝯?', `${label} stale photo confirm guard copy`);
  assertIncludes(html, 'photoSessionId: context.photoSessionId || activePhotoSessionId', `${label} photo API session context`);
  assertIncludes(html, 'getEstimateStabilityWarning', `${label} unstable estimate warning helper`);
  assertIncludes(html, 'rememberEstimateForStability', `${label} estimate stability memory`);
  assertIncludes(html, 'applyClientEstimateGuard', `${label} client-side visual estimate guard`);
  assertIncludes(html, 'applyEstimateConsistencyGuard', `${label} repeated photo consistency guard`);
  assertIncludes(html, 'getRecentEstimateForStability', `${label} recent estimate memory reader`);
  assertIncludes(html, '_stability_guard', `${label} repeated photo stability source marker`);
  assertIncludes(html, '???挾??隡啁?撌桀云憭?, `${label} repeated estimate correction copy`);
  assertIncludes(html, '_client_drink_guard', `${label} client drink guard provider`);
  assertIncludes(html, '撌脤甇ａㄡ?◤隤文???520 kcal 甇??', `${label} client drink guard copy`);
  assertIncludes(html, '???挾??隡啁?撌?, `${label} repeated estimate variance copy`);
  assertIncludes(html, '?航??AI ?擗??嗅??湧?', `${label} after-photo full-meal guard copy`);
  assertIncludes(html, '憌臬?瘥??箸?', `${label} after photo should carry before-meal context`);
  assertIncludes(html, "providerSource = payload.provider || payload.source || 'ai_photo'", `${label} photo provider source should be preserved`);
  assertIncludes(html, 'estimateAfterPhotoLocally', `${label} local after-photo fallback`);
  assertIncludes(html, 'local_after_photo_fallback', `${label} local after-photo fallback source`);
  assertIncludes(html, 'currentMealPlan = { ...meal.mealPlan }', `${label} after photo restores meal plan`);
  assertIncludes(html, 'const beforePhotoSrc = pending.photoBefore || pending.photo || base.photoBefore || "";', `${label} after photo preserves original before photo`);
  assertIncludes(html, 'photoBefore: beforePhotoSrc', `${label} after photo writes preserved before photo`);
  assertIncludes(html, 'showInstantPhotoEstimateIfPossible', `${label} instant photo estimate helper`);
  assertIncludes(html, 'getPhotoEstimateHint', `${label} instant estimate hint helper`);
  assertIncludes(html, '?擗?敹恍摯蝞?, `${label} instant estimate copy`);
  assertIncludes(html, 'activePhotoSessionId = Date.now();', `${label} cancel late AI after confirm`);
  assertIncludes(html, 'getTodayMealRecordsForAfterPhoto', `${label} today-first after-photo source helper`);
  assertIncludes(html, '撌脤摰?嚗?ㄞ敺???貊倏鋆', `${label} after-photo source choice toast`);
  assertIncludes(html, "openPhotoSourceSheet('after')", `${label} after-photo opens source sheet`);
  assertIncludes(html, '憌臬??批歇鋆脤?', `${label} after photo rescue copy`);
  assertIncludes(html, 'savedMealId: pending.savedMealId || pending.id', `${label} pending meal id fallback`);
  assertIncludes(html, '憌臬?鋆?', `${label} after-meal photo copy`);
  assertIncludes(html, '憛?撟思????, `${label} decision summary copy`);
  assertIncludes(html, 'renderDecisionActionBridge', `${label} decision action bridge renderer`);
  assertIncludes(html, 'const requestedQuestion = String(presetText || \'\').trim();', `${label} coach question normalization`);
  assertIncludes(html, 'if (requestedQuestion && input) input.value = requestedQuestion;', `${label} coach preset writes visible input`);
  assertIncludes(html, 'function askTataCoach(presetText = "")', `${label} coach answer handler`);
  assertIncludes(html, "switchTabById('tab-photo');", `${label} coach answer opens visible photo tab`);
  assertIncludes(html, 'decision-action-bridge', `${label} decision action bridge style`);
  assertIncludes(html, '憛?????', `${label} decision action bridge copy`);
  assertIncludes(html, '憛??湔撟急???, `${label} decision-to-photo CTA`);
  assertIncludes(html, 'renderDecisionSummary', `${label} decision summary renderer`);
  assertIncludes(html, 'renderNextMealDecisionPanel', `${label} next meal decision panel renderer`);
  assertIncludes(html, 'renderDecisionRadar', `${label} decision radar renderer`);
  assertIncludes(html, '憛?瘙箇??琿?', `${label} decision radar copy`);
  assertIncludes(html, 'decision-radar-grid', `${label} decision radar grid style`);
  assertIncludes(html, '?抒?蝣箄?', `${label} decision radar photo check`);
  assertIncludes(html, 'route: "tata_decision"', `${label} selected TATA decision persists as meal plan`);
  assertIncludes(html, 'getNutritionGapDiagnosis', `${label} nutrition gap diagnosis helper`);
  assertIncludes(html, 'getTataMealTemplates', `${label} TATA meal template helper`);
  assertIncludes(html, 'fiberPer1000Kcal: 14', `${label} official fiber per calorie baseline`);
  assertIncludes(html, 'AI ??憌???遢??靘捆?典之撠瘙?皝舀??ㄡ?車憿?隞亙?憌臬?憌臬??拚???, `${label} AI monitoring explanation copy`);
  assertIncludes(html, 'id="selectedDecisionCard"', `${label} selected decision card`);
  assertIncludes(html, 'renderSelectedDecisionCard', `${label} selected decision renderer`);
  assertIncludes(html, '憛?撌脤憟?, `${label} selected decision copy`);
  assertIncludes(html, 'id="mealDecisionPathCard"', `${label} meal decision path card`);
  assertIncludes(html, 'renderMealDecisionPathCard', `${label} meal decision path renderer`);
  assertIncludes(html, 'getMealDecisionPaths', `${label} meal decision path helper`);
  assertIncludes(html, 'selectMealDecisionPath', `${label} meal decision path selector`);
  assertIncludes(html, 'currentMealPlan', `${label} current meal plan state`);
  assertIncludes(html, "dailyKey('currentMealPlan')", `${label} current meal plan persistence`);
  assertIncludes(html, 'mealPlan: meal.mealPlan || null', `${label} meal plan saved on records`);
  assertIncludes(html, 'clearCurrentMealPlan', `${label} current meal plan cleanup`);
  assertIncludes(html, 'mealText,', `${label} AI estimate receives meal plan hint`);
  assertIncludes(html, '銝瘙箏?嚗?憛策銝?頝?, `${label} meal decision path copy`);
  assertIncludes(html, '?典振??, `${label} home-cook decision path`);
  assertIncludes(html, '憭?暺?, `${label} eat-out decision path`);
  assertIncludes(html, '????, `${label} nearby decision path`);
  assertIncludes(html, 'decision-card-tags', `${label} decision option tags`);
  assertIncludes(html, 'getDecisionOptionTags', `${label} decision option tag helper`);
  assertIncludes(html, 'decision-card-reasons', `${label} decision option reasons style`);
  assertIncludes(html, 'getDecisionOptionDetail', `${label} decision option detail helper`);
  assertIncludes(html, 'renderDecisionOptionDetails', `${label} decision option detail renderer`);
  assertIncludes(html, '隞?', `${label} decision option why copy`);
  assertIncludes(html, '?抒?蝣箄?', `${label} decision option photo check copy`);
  assertIncludes(html, 'decisionDetail: getDecisionOptionDetail', `${label} selected decision saves detail`);
  assertIncludes(html, 'getMealDecisionMemory', `${label} meal decision memory helper`);
  assertIncludes(html, '憛?瘙箇?嚗?, `${label} today ledger decision memory copy`);
  assertIncludes(html, '?嗆??嚗?, `${label} history decision reason copy`);
  assertIncludes(html, '?抒?蝣箄?嚗?, `${label} photo modal decision photo check copy`);
  assertIncludes(html, '?曉?ㄞ?', `${label} selected decision photo CTA`);
  assertIncludes(html, '?暸?餈??', `${label} selected decision nearby CTA`);
  assertIncludes(html, 'AI ???靘祕?遢?甇?, `${label} no fixed portion decision copy`);
  assertIncludes(html, 'FDA/DGA/DRI ?祇??箸?', `${label} official baseline copy`);
  assertIncludes(html, 'proteinDvG: 50', `${label} official protein daily value floor`);
  assertIncludes(html, 'decision-summary', `${label} decision summary style`);
  assertIncludes(html, 'AI ?????, `${label} visible detected food copy`);
  assertIncludes(html, '皜祈岫?閬撓?亙董??, `${label} passwordless login copy`);
  assertExcludes(html, '憌臬??亥?', `${label} stale login copy`);
  assertExcludes(html, '撠惇撽?撖Ⅳ', `${label} stale password copy`);
  assertExcludes(html, 'Toma2026', `${label} stale password hint`);

  const navLabels = ["?ㄞ", "頨恍?", "?ㄞ??, "?貊倏", "頞典"];
  for (const navLabel of navLabels) assertIncludes(html, navLabel, `${label} bottom nav ${navLabel}`);

  assert.strictEqual(countMatches(html, /class="bottom-tab-btn/g), 4, `${label} should have 4 tab buttons plus center camera action`);
  assert.strictEqual(countMatches(html, /class="bottom-photo-action/g), 1, `${label} should have one center camera action`);
}

function checkServiceWorker() {
  const sw = readLocal("sw.js");
  assertIncludes(sw, 'const CACHE_NAME = "paipachi-pwa-v214"', "service worker cache version");
  assertIncludes(sw, '"./styles.css"', "service worker should cache extracted stylesheet");
  assertIncludes(sw, '"./app.js"', "service worker should cache extracted app script");
  assertIncludes(sw, 'url.pathname.startsWith("/api/")', "service worker API bypass");
}

function checkExtractedAssets(html, css, js) {
  assertIncludes(html, '<link rel="stylesheet" href="./styles.css?v=214">', "index should load extracted stylesheet");
  assertIncludes(html, '<script src="./app.js?v=214"></script>', "index should load extracted app script");
  assert.ok(!/<style>[\s\S]{1000,}<\/style>/.test(html), "index should not contain a large inline style block");
  assert.ok(!/<script>[\s\S]{1000,}<\/script>/.test(html), "index should not contain a large inline script block");
  assert.ok(Buffer.byteLength(html) < 90000, "index shell should stay small enough to cache quickly");
  assert.ok(Buffer.byteLength(css) > 100000, "extracted stylesheet should contain the app CSS");
  assert.ok(Buffer.byteLength(js) > 300000, "extracted app script should contain the app logic");
}

function checkServerPrompts() {
  const server = readLocal("server.js");
  assertIncludes(server, "銝?????蝣箏??抒??箏?隡唳? 520 kcal", "OpenAI no fixed 520 prompt");
  assertIncludes(server, "銝????暺?暺摯蝞??拙?蝔晞nknown??, "Gemini no generic food prompt");
  assertIncludes(server, "憒??抒??ㄡ??", "drink can prompt guard");
  assertIncludes(server, "擗??拚??摯蝞?, "after-meal remaining prompt");
  assertIncludes(server, "normalizeUnstableVisualEstimate", "unstable visual guard");
  assertIncludes(server, "isAfterMeal", "after-photo server scenario guard");
  assertIncludes(server, "\\u98ef\\u5f8c\\u7167\\u5df2\\u8b80\\u53d6", "after-photo fallback guidance");
  assertIncludes(server, "v76 stable final overrides", "stable final calorie override section");
  assertIncludes(server, "mealRule(\"\\u53ef\\u6a02\"", "cola should have drink-specific calories");
  assertIncludes(server, "mealRule(\"\\u97d3\\u5f0f\\u6ce1\\u83dc\\u6e6f\"", "kimchi soup should have soup-specific calories");
  assertIncludes(server, "after|\\u9910\\u5f8c|\\u5269\\u9918|\\u5403\\u5b8c", "after-photo should avoid fast text but keep fallback estimate");
  assertIncludes(server, '["slim", "fitness", "maintain", "healthy", "gain"]', "server should persist all MVP goal types");
  assertIncludes(server, "placeName", "stored meal place name");
  assertIncludes(server, "locationLatitude", "stored meal location latitude");
  assertIncludes(server, "locationLongitude", "stored meal location longitude");
  assertIncludes(server, "placeRating", "stored meal place rating");
  assertIncludes(server, "normalizeStoredMealPlan", "stored meal plan normalizer");
  assertIncludes(server, "normalizeStoredDecisionDetail", "stored meal decision detail normalizer");
  assertIncludes(server, "mealPlan: normalizeStoredMealPlan(value.mealPlan)", "stored meal keeps meal plan");
}

function checkManifest() {
  const manifest = JSON.parse(readLocal("manifest.webmanifest"));
  assert.strictEqual(manifest.name, "拍拍吃", "manifest app name");
  assert.strictEqual(manifest.short_name, "PaiPaiChi", "manifest short name");
  assert.ok(manifest.description.includes("你的照片營養師"), "manifest description should mention photo nutritionist");
  assert.ok(manifest.description.includes("塔塔"), "manifest description should keep TATA character name");
  assert.ok(manifest.icons.some((icon) => icon.src === "./icon.svg"), "manifest should include SVG icon");
  const iconSvg = readLocal("icon.svg");
  assertIncludes(iconSvg, "A calm cocoa-brown otter mascot holding a ripe tomato", "TATA tomato icon description");
}

async function main() {
  const localHtml = readLocal("index.html");
  const localCss = readLocal("styles.css");
  const localJs = readLocal("app.js");
  const localBundle = `${localHtml}\n${localCss}\n${localJs}`;
  checkExtractedAssets(localHtml, localCss, localJs);
  checkHtml(localBundle, "local app bundle");
  checkServiceWorker();
  checkServerPrompts();
  checkManifest();

  const remote = await fetchText(`${baseUrl}/`);
  assert.strictEqual(remote.response.status, 200, "served page should be 200");
  assertIncludes(remote.text, './styles.css?v=214', `served ${baseUrl} stylesheet link`);
  assertIncludes(remote.text, './app.js?v=214', `served ${baseUrl} app script link`);

  console.log(JSON.stringify({
    ok: true,
    baseUrl,
    checked: [
      "v213 app shell",
      "passwordless login copy",
      "before/after photo inputs and bottom nav overlay guard",
      "5 bottom navigation entries",
      "today diary/meal ledger/place memory/water reminder/auto step/photo draft/daily action hub/decision memory/meal flow surfaces",
      "detected food copy",
      "service worker cache/API bypass",
      "clean PWA manifest and TATA tomato icon",
    ],
  }, null, 2));
}

main().catch((error) => {
  console.error(JSON.stringify({ ok: false, baseUrl, message: error.message, stack: error.stack }, null, 2));
  process.exit(1);
});
